# pnpm 10 refuses a dependency's install scripts until the package is named
# here. esbuild's postinstall places the platform binary its CLI wrapper runs;
# without it the build script fails on a clean install.
onlyBuiltDependencies:
  - esbuild
